<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Libretime Blog</title>
        <link>https://libretime.org/libretime/blog</link>
        <description>Libretime Blog</description>
        <lastBuildDate>Fri, 15 Oct 2021 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <item>
            <title><![CDATA[Libretime 3.0 Alpha 10 Released]]></title>
            <link>https://libretime.org/libretime/blog/3.0-alpha-10-released</link>
            <guid>3.0-alpha-10-released</guid>
            <pubDate>Fri, 15 Oct 2021 00:00:00 GMT</pubDate>
            <description><![CDATA[The complete LibreTime documentation is available at libretime.org.]]></description>
            <content:encoded><![CDATA[<p>The complete LibreTime documentation is available at libretime.org.</p><p>The full tarball for the <strong>3.0.0-alpha.10</strong> release of LibreTime is available <a href="https://github.com/LibreTime/libretime/releases/download/3.0.0-alpha.10/libretime-3.0.0-alpha.10.tar.gz">here</a>.</p><p>Since this is an alpha release there will be bugs in the code.</p><p>Please report new issues and/or feature requests in the <a href="https://github.com/LibreTime/libretime/issues">issue tracker</a>. Join our <a href="https://discourse.libretime.org/">discourse</a> or chat to us on our <a href="https://chat.libretime.org/e">Mattermost</a> instance if you need help and for general discussion.</p><h2>Features</h2><ul><li>Support <code>force_ssl</code> configuration option in Python applications</li><li>Move <code>airtime_mvc</code> to <code>legacy</code> and move all PHP related files under it</li><li>Support <code>Authorization: Api-Key</code> header in API v1</li><li>Use pip for LibreTime Python package installation</li><li>Move Python scripts into <code>/usr/local/bin</code></li><li>Add REST API v2 (unstable and subject to change)</li></ul><h2>Bug Fixes</h2><ul><li>Renamed airtime_analyzer to libretime-analyzer</li><li>Prevent autoload playlists running on deleted show instances</li><li>Playout history can be exported as CSV and PDF</li><li>Explicitly fail if the HTTP requests made by the Python applications fail</li><li>Fix API v2 schedule endpoint item filtering and overlapping playout</li><li>Fix pypo overlapping track playout</li><li>Fix installation when Icecast is already installed</li><li>Request 1Gb of memory on libvirt Vagrant boxes</li><li>Clean up CORS setup in the installer</li><li>Pin the <code>setuptools</code> version to ensure older versions of LibreTime can still be installed</li></ul><h2>Deprecated Features</h2><ul><li>Removed broken Soundcloud integration</li><li>Dropped support for Ubuntu Xenial as it is end-of-life</li><li>Dropped support for Debian Stretch as it is end-of-life</li><li>Removed SysV and Upstart init system files</li><li>Removed broken My Podcasts feature</li></ul><h2>Contributors</h2><p>The LibreTime project wants to thank the following contributors for authoring PRs to this release:</p><ul><li><a href="https://github.com/jooola">@jooola</a></li><li><a href="https://github.com/paddatrapper">@paddatrapper</a></li><li><a href="https://github.com/xabispacebiker">@xabispacebiker</a></li><li><a href="https://github.com/malespiaut">@malespiaut</a></li><li><a href="https://zklosko.github.io">@zklosko</a></li><li><a href="https://github.com/brekemeier">@brekemeier</a></li><li><a href="https://github.com/jeromelebleu">@jeromelebleu</a></li><li><a href="https://github.com/danielhjames">@danielhjames</a></li><li><a href="https://github.com/rjhelms">@rjhelms</a></li><li><a href="https://github.com/hairmare">@hairmare</a></li></ul><h2>Installation</h2><p>The main installation docs may be found <a href="/docs/getting-started/install/">here</a>. They describe a &quot;developer&quot; install using the bundled <code>install</code> script.</p><p>We are preparing packages for supported distros and you can take those for a spin if you would like to. Usually the packages get built pretty soon after a release is published. If the current version is not available from the below sources you should wait for a while until they get uploaded.</p><ul><li><a href="https://github.com/LibreTime/libretime-debian-packaging/releases">Ubuntu packages</a></li><li><a href="https://github.com/LibreTime/libretime-debian-packaging/releases">Debian packages</a></li><li><a href="https://build.opensuse.org/package/show/home:radiorabe:airtime/libretime">CentOS packages</a></li></ul><p>Please reference these links for further information on how to install from packages. The install docs will get updated to show how to install packages once we have validated that the packages work properly and when the packages are available from a repository allowing you to automate updating to a new version.</p><p>If you want to skip the installer GUI completely you can configure LibreTime using <code>legacy/build/airtime.example.conf</code> as an template. Due to some Python/PHP differences you must remove all comments from the example to use it 😞. You&#x27;ll also have to create some folder structures manually and check if the music dir got properly created directly in the database. Referencing a second install -fiap install on a non productive system for reference can help with this type of bootstrap.</p><h2>Updating</h2><p>See <a href="/docs/upgrading">the docs</a> for complete information on updating. Please ensure that you have proper backups and a rollback scenario in place before updating.
If the update does not go smoothly, it may cause significant downtime, so you should always have a fallback system available during the update to ensure broadcast continuity.</p><p>If you installed from GitHub you can <code>git pull</code> in your local working copy and re-run the <code>./install</code> script with the same <code>--web-root</code> and <code>--web-user</code> arguments you used during the initial install. Tarball users can leave out the git pull part and just call the new version of the install script.</p><p>Once the update has taken place, you will need to run the following commands to clean up old scripts and configuration:</p><pre><code class="language-shell"># Remove the old packages
sudo pip3 uninstall \
  airtime-playout \
  airtime-celery \
  api_clients

# Remove old entrypoints
sudo rm -f \
  /usr/bin/airtime-liquidsoap \
  /usr/bin/airtime-playout \
  /usr/bin/pyponotify

# Remove old config files
sudo rm -f \
  /etc/logrotate.d/airtime-liquidsoap

# Remove the old runtime and PHP directories
sudo rm -rf \
  /var/run/airtime \
  /run/airtime \
  /usr/share/airtime/php/airtime_mvc

# Remove old python libraries entrypoints
sudo rm -f \
  /usr/bin/airtime-liquidsoap \
  /usr/bin/airtime-playout \
  /usr/bin/libretime-analyzer \
  /usr/bin/libretime-api \
  /usr/bin/collectiongain \
  /usr/bin/django-admin \
  /usr/bin/django-admin.py \
  /usr/bin/markdown_py \
  /usr/bin/mid3cp \
  /usr/bin/mid3iconv \
  /usr/bin/mid3v2 \
  /usr/bin/moggsplit \
  /usr/bin/mutagen-inspect \
  /usr/bin/mutagen-pony \
  /usr/bin/pyponotify \
  /usr/bin/replaygain \
  /usr/bin/sqlformat
</code></pre><h2>Known Issues</h2><p>The following issues may need a workaround for the time being. Please search <a href="https://github.com/LibreTime/libretime/issues">the issues</a> before reporting problems not listed below.</p><h3>Interface Customization Issues</h3><p>The UI works best if you don&#x27;t use it in an opinionated fashion and change just the bare minimal.</p><h3>No watched folder support</h3><p>Currently LibreTime does not support watching folders. Uploading files through the web interface works fine and can be automated via a REST API. Re-implementing watched folder support is on the roadmap. Please consider helping out with the code to help speed things along if you want to use the feature. This is tracked in <a href="https://github.com/LibreTime/libretime/issues/70">#70</a>.</p><h3>No line in support</h3><p>This feature went missing from LibreTime due to the fact that we based our code off of the saas-dev branch of legacy upstream and support for recording hasn&#x27;t been ported to the new airtime analyzer ingest system. #42 currently tracks the progress being made on line in recording. This is tracked in <a href="https://github.com/LibreTime/libretime/issues/42">#42</a>.</p><h3>Playout won&#x27;t work if locale is missing</h3><p>Some minimal OS installs do not have a default locale configured. This only seems to affect some VPS installs as they often do not have a locale setup in the default images provided. This is tracked in <a href="https://github.com/LibreTime/libretime/issues/317">#317</a>.</p><p>You can set up the locale using a combination of the following commands. You might also want to consult the documentation of your VPS provider as it may contain an official way to set up locales when provisioning a VPS.</p><pre><code class="language-shell"># Set locale using systemds localectl
localectl set-locale LANG=&quot;en_US.utf8&quot;
</code></pre><p>These instructions do not seem to work on all Debian based distros so you might need to use update-locale as follows.</p><pre><code class="language-shell">#Purge all locales but en_US.UTF-8
sudo locale-gen --purge en_US.UTF-8
#Populate LANGUAGE=
sudo update-locale LANGUAGE=&quot;en_US.UTF-8&quot;
</code></pre>]]></content:encoded>
        </item>
    </channel>
</rss>